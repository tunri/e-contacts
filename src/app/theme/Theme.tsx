// import { useMemo } from "react";

// import { createTheme } from "@mui/material/styles";

const Theme = {
	palette: {
		mode: "dark",
		primary: {
			main: "blue",
		},
	},
};

export default Theme;
